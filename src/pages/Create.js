import React from "react";
import { Typography } from "@material-ui/core";

const Create = () => {
  return (
    <div>
      <Typography
        variant="h6"
        color="textSecondary"
        component="h2"
        gutterBottom
      >
        Create a New Note
      </Typography>
    </div>
  );
};

export default Create;
